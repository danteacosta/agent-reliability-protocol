# Contributing

Install with `python -m pip install -e '.[test]'` (or the repository's
declared development command) and run `python -m pytest -q`. Preserve wire
compatibility and add contract tests for schema changes.
